---
// German story route. Explicit file rather than a dynamic `[lang]` route, for
// the same reason as `de/index.astro` — see the note there.
import StoryPage from '../../components/StoryPage.astro';
---

<StoryPage locale="de" />
